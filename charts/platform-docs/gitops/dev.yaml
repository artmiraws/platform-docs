# Desired dev image, updated by CI (.github/workflows/deploy-dev.yml). Argo CD reconciles it.
# Only the image digest lives here; the rest of the wiring (repository, ingress, certificate) is
# injected by the platform so no account IDs, ARNs, or hostnames are committed.
image:
  digest: "sha256:72501037340f14069c1a4a677f47c83ea0c00cd65b4aa6613c964ec21a30632f"
